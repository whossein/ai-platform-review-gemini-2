# Plugins

First-party and third-party plugins live here (workspace glob: `packages/plugins/*`).
Each plugin depends only on `@ai-review/core` and registers capabilities via the plugin contract. See docs/PLUGIN_GUIDE.md.
