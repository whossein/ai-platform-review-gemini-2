{
  "name": "@ai-review/orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "Review composition root: rules → planner → agents → critic → judge → report.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@ai-review/core": "*",
    "@ai-review/agent-runtime": "*",
    "@ai-review/context-engine": "*",
    "@ai-review/config": "*",
    "@ai-review/llm": "*",
    "@ai-review/memory": "*",
    "@ai-review/reporting": "*",
    "@ai-review/shared": "*",
    "@ai-review/workflow-engine": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
